import middle from "../assets/middle.jpg"; 

const MidSection = () => {
  return (
    <section className="mid-section-container" style={{ backgroundImage: `url(${middle})` }}>
      <div className="MidSection">
      </div>
    </section>
  );
};

export default MidSection;